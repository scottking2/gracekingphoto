---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="Contact - Grace King Photography">
  <Header />
  <main class="pt-[92px]">
    
    <!-- Contact Section -->
    <section class="bg-cream py-20 md:py-32 px-8 min-h-screen flex items-center">
      <div class="max-w-3xl mx-auto w-full">
        <h1 class="font-serif text-4xl md:text-5xl text-center mb-12">Hooray! Let's do this!</h1>
        
        <p class="text-lg md:text-xl leading-relaxed text-justify mb-12">
          My photoshoot dates are available on a first come, first serve basis. Dates will be reserved by submitting a complete contract and a retainer of $200. Shoot me an email and let's set up a time to chat and get to know one another! I can't wait to work with you to create beautiful photographic heirlooms you will treasure.
        </p>

        <!-- Contact Info -->
        <div class="text-center space-y-6">
          <div>
            <h3 class="text-sm uppercase tracking-wider mb-2">Email</h3>
            <a href="mailto:grace@gracekingphoto.com" class="text-lg hover:opacity-70 transition-opacity">
              grace@gracekingphoto.com
            </a>
          </div>
          
          <div>
            <h3 class="text-sm uppercase tracking-wider mb-2">Instagram</h3>
            <a href="https://instagram.com/gracekingphoto" target="_blank" rel="noopener" class="text-lg hover:opacity-70 transition-opacity">
              @gracekingphoto
            </a>
          </div>
        </div>

        <!-- Simple Contact Form -->
        <form class="mt-16 space-y-6">
          <div>
            <label for="name" class="block text-sm uppercase tracking-wider mb-2">Name</label>
            <input 
              type="text" 
              id="name" 
              name="name" 
              required 
              class="w-full px-4 py-3 bg-white border border-black/20 focus:border-black focus:outline-none transition-colors"
            />
          </div>

          <div>
            <label for="email" class="block text-sm uppercase tracking-wider mb-2">Email</label>
            <input 
              type="email" 
              id="email" 
              name="email" 
              required 
              class="w-full px-4 py-3 bg-white border border-black/20 focus:border-black focus:outline-none transition-colors"
            />
          </div>

          <div>
            <label for="phone" class="block text-sm uppercase tracking-wider mb-2">Phone (Optional)</label>
            <input 
              type="tel" 
              id="phone" 
              name="phone" 
              class="w-full px-4 py-3 bg-white border border-black/20 focus:border-black focus:outline-none transition-colors"
            />
          </div>

          <div>
            <label for="message" class="block text-sm uppercase tracking-wider mb-2">Message</label>
            <textarea 
              id="message" 
              name="message" 
              required 
              rows="6" 
              class="w-full px-4 py-3 bg-white border border-black/20 focus:border-black focus:outline-none transition-colors resize-none"
            ></textarea>
          </div>

          <button 
            type="submit" 
            class="w-full py-4 bg-light-brown text-white uppercase tracking-wider hover:bg-light-brown/90 transition-colors"
          >
            Send Message
          </button>

          <p class="text-sm text-center text-black/60">
            Form submissions coming soon. Please email directly for now.
          </p>
        </form>
      </div>
    </section>

  </main>
  <Footer />
</Layout>
